[project]
name = "sql-practice"
version = "0.1.0"
description = "Interactive terminal SQL learning app"
requires-python = ">=3.10"
dependencies = [
    "textual>=0.45.0",
]

[project.scripts]
practice = "practice:main"

